#include<stdio.h>
int insertion_array(int arr);
int deletion_array(int arr);
int traversing_array( int arr);
int array_( int arr);

int array_(int arr)
{
    int size,i;
    printf("Select the size of array");
    scanf("%d",&size);
    int arr[size];
    for(i=0;i<=size;i++)
    {
        scanf(%d,arr[i]);
    }
    return arr[i];
}
